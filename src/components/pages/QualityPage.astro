---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHead from '../PageHead.astro';
import DocLayout from '../DocLayout.astro';
import DocBlock from '../DocBlock.astro';
import Icon from '../Icon.astro';
import type { IconName } from '../Icon.astro';
import { useContent, type Lang } from '../../i18n';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useContent(lang);
const q = t.quality;

const nav = [
  { label: q.control.title, hash: q.control.id },
  { label: q.certsBlock.title, hash: q.certsBlock.id },
  { label: q.membership.title, hash: q.membership.id },
  { label: q.trademark.title, hash: q.trademark.id },
];

// CLS'i önlemek için gerçek görsel oranı: sertifikaların çoğu A4 (566x800), I-REC belgeleri farklı orana sahip (800x1180).
const certSize = (img: string) => (img.includes('irec') ? { w: 800, h: 1180 } : { w: 566, h: 800 });
---

<BaseLayout lang={lang} routeKey="quality" title={t.meta.quality.title} description={t.meta.quality.description} image="/img/kalite-2.webp">
  <PageHead lang={lang} crumb={t.nav.quality} title={t.nav.quality} lead={q.lead} bg="/img/kalite-2.webp" />
  <DocLayout nav={nav}>
    <DocBlock
      id={q.control.id}
      kicker={q.control.kicker}
      title={q.control.title}
      paras={q.control.paras}
      img={q.control.img}
      imgAlt={q.control.imgAlt}
    />

    <article class="doc-block" id={q.certsBlock.id} data-reveal>
      <div class="kicker">{q.certsBlock.kicker}</div>
      <h2>{q.certsBlock.title}</h2>
      <p>{q.certsBlock.intro}</p>
      <div class="grid grid-3" style="margin-top:30px;">
        {
          q.certsBlock.items.map((c, i) => (
            <div class="cert cert-v2">
              <div class="cert-badge-row">
                <span class="cert-icon"><Icon name={c.icon as IconName} /></span>
                <span class="cert-verified" aria-hidden="true"><Icon name="check" /></span>
              </div>
              <button type="button" class="cert-zoom" data-cert-trigger={i} aria-label={lang === 'tr' ? 'Büyüt' : 'Zoom in'}>
                <img src={c.img} alt={`${c.code} ${c.name}`} width={certSize(c.img).w} height={certSize(c.img).h} loading="lazy" />
              </button>
              <h3>
                {c.code}
                <br />
                {c.name}
              </h3>
              <p class="cert-desc">{c.desc}</p>
            </div>
          ))
        }
      </div>

      <div class="cert-stats-band">
        <div class="kicker">{q.certsBlock.statsBand.kicker}</div>
        <h3>{q.certsBlock.statsBand.title}</h3>
        <p>{q.certsBlock.statsBand.text}</p>
        <div class="cert-stats">
          {
            q.certsBlock.statsBand.stats.map((s) => (
              <div class="cert-stat">
                <span class="n">{s.n}</span>
                <span class="l">{s.l}</span>
              </div>
            ))
          }
        </div>
      </div>
    </article>

    <div class="lightbox" id="cert-lightbox" hidden>
      <button type="button" class="lightbox-close" data-cert-close aria-label={lang === 'tr' ? 'Kapat' : 'Close'}>
        <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M6 6 18 18M18 6 6 18" /></svg>
      </button>
      <button type="button" class="lightbox-nav prev" data-cert-prev aria-label={lang === 'tr' ? 'Önceki' : 'Previous'}>
        <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><path d="M15 5 L8 12 L15 19" stroke-linecap="round" stroke-linejoin="round" /></svg>
      </button>
      <img src="" alt="" />
      <button type="button" class="lightbox-nav next" data-cert-next aria-label={lang === 'tr' ? 'Sonraki' : 'Next'}>
        <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><path d="M9 5 L16 12 L9 19" stroke-linecap="round" stroke-linejoin="round" /></svg>
      </button>
    </div>

    <script>
      function initCertLightbox() {
        const lightbox = document.getElementById('cert-lightbox');
        const triggers = Array.from(document.querySelectorAll<HTMLButtonElement>('[data-cert-trigger]'));
        if (!lightbox || triggers.length === 0) return;

        const lbImg = lightbox.querySelector('img');
        let current = 0;

        function open(index: number) {
          current = (index + triggers.length) % triggers.length;
          const img = triggers[current].querySelector('img');
          if (lbImg && img) {
            lbImg.src = img.src;
            lbImg.alt = img.alt;
          }
          lightbox?.removeAttribute('hidden');
        }
        function close() {
          lightbox?.setAttribute('hidden', '');
        }

        triggers.forEach((btn, i) => btn.addEventListener('click', () => open(i)));
        lightbox.querySelector('[data-cert-close]')?.addEventListener('click', close);
        lightbox.querySelector('[data-cert-prev]')?.addEventListener('click', () => open(current - 1));
        lightbox.querySelector('[data-cert-next]')?.addEventListener('click', () => open(current + 1));
        lightbox.addEventListener('click', (e) => {
          if (e.target === lightbox) close();
        });
        document.addEventListener('keydown', (e) => {
          if (lightbox.hasAttribute('hidden')) return;
          if (e.key === 'Escape') close();
          if (e.key === 'ArrowLeft') open(current - 1);
          if (e.key === 'ArrowRight') open(current + 1);
        });
      }
      initCertLightbox();
    </script>

    <article class="doc-block" id={q.membership.id} data-reveal>
      <div class="kicker">{q.membership.kicker}</div>
      <h2>{q.membership.title}</h2>
      <p>{q.membership.body}</p>
    </article>

    <article class="doc-block" id={q.trademark.id} data-reveal>
      <div class="kicker">{q.trademark.kicker}</div>
      <h2>{q.trademark.title}</h2>
      <p>{q.trademark.body}</p>
      <div class="doc-media">
        <img src={q.trademark.img} alt={q.trademark.imgAlt} width="520" height="800" loading="lazy" decoding="async" />
      </div>
    </article>
  </DocLayout>
</BaseLayout>

<style>
  .cert-badge-row {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 14px;
  }
  .cert-icon {
    display: inline-flex; align-items: center; justify-content: center;
    width: 40px; height: 40px; border-radius: 3px;
    border: 1px solid var(--line); color: var(--ink);
  }
  .cert-icon :global(svg) { width: 19px; height: 19px; }
  .cert-verified {
    display: inline-flex; align-items: center; justify-content: center;
    width: 26px; height: 26px; border-radius: 50%;
    border: 1px solid var(--line-soft); color: var(--steel-2);
  }
  .cert-verified :global(svg) { width: 13px; height: 13px; }

  .cert-stats-band {
    margin-top: 40px;
    background: var(--ink);
    color: #fff;
    padding: 44px 40px;
    text-align: center;
  }
  .cert-stats-band .kicker { justify-content: center; color: var(--steel-2); }
  .cert-stats-band .kicker::before { background: var(--steel-2); }
  .cert-stats-band h3 { font-size: clamp(1.4rem, 2.6vw, 1.9rem); font-weight: 700; margin-bottom: 12px; }
  .cert-stats-band p { color: #c4cbd4; max-width: 56ch; margin: 0 auto; }
  .cert-stats {
    display: flex; justify-content: center; gap: 48px; flex-wrap: wrap;
    margin-top: 30px; padding-top: 28px; border-top: 1px solid rgba(255, 255, 255, .16);
  }
  .cert-stat { display: flex; flex-direction: column; align-items: center; gap: 6px; }
  .cert-stat .n { font-family: var(--ff-display); font-weight: 700; font-size: clamp(1.6rem, 3vw, 2.2rem); color: var(--steel-2); }
  .cert-stat .l { font-size: .84rem; color: #aeb7c2; text-transform: uppercase; letter-spacing: .08em; }
</style>
